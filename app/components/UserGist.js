var React     = require('react');
var PropTypes = React.PropTypes;

var UserGist = React.createClass({

    render: function () {
        return (
            <div>
            </div>
        );
    }
});

module.export = UserGist;

